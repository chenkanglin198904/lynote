# LyNote 使用说明

[English README](README.md) · [简体中文 README](README.zh-CN.md)

给第一次打开工作台的人：这是什么、能做什么、该按什么顺序用。开发与部署见 README，产品约束见 [PROJECT.md](./PROJECT.md)。

打开工作台后，顶栏有 **用法**，会进入同一份说明的网页版。

---

## 这是什么

LyNote 是个人外脑知识平台。它帮你做四件事：

1. **入库**：从随手记、网页、Markdown、PDF、音频、视频里筛出对当前主题有用的材料。
2. **召回**：在需要时，按主题找出带出处的主张，而不是搜一篇流畅的空话。
3. **决策**：把选项、证据、未知、最强反对意见摊开，你拍板并写回结果。
4. **学习**：用图上的主张讲解 → 追问你 → 批改 → 把你的错法挂到正确主张上，下次相关问题时强制带回。

质量看**拒绝率**和**能不能点开原文核对**，不看摄入量，也不看对话是否流畅。

## 这不是什么

- 不是 ChatGPT 套壳，不会在图外编百科标准答案。
- 不是笔记全文搜索：没有主张、冲突、决策，就不算本产品。
- 不是全量 embedding 仓库：过闸默认拒绝无关和清单体。
- 不是世界模拟、千人社会、多用户云同步。

答不上来会写 **未知**。这是功能，不是故障。

---

## 第一次打开会看到什么

三栏工作台：

| 位置 | 名称 | 做什么 |
|---|---|---|
| 左 | 今日桌面 | 当前主题、随手记、下一步（当前主题、最多三张主卡）；「送进来 / 掌握 / 偏好」折在下面 |
| 中 | 导图 + 搜索 | 总览：主题在上、下一层分排；点进去只留祖先路径，其它兄弟层收起 |
| 右 | 决策简报 / 学习 / 出处 | 拍板、学知识点、核对原文高亮 |

顶栏还有 **学习频道**：左栏按图上已有概念拆成目录，中间仍是导图，右侧默认出处，可切到问答。问答里的追问仍是图上主张；有依据的回答可「收入当前章节」，只连已有主张，不编新知识点。

首次会带一个样例主题「图谱还是笔记堆」，以及一份被拒绝的营销清单，用来演示过闸。

左栏是 **今日桌面**：当前主题和随手记始终在上面。「下一步」只列当前主题，顺序是待挂、待审、到期巩固，然后是已拍板待复盘；简报草稿只在你本周主动重算过之后才出现。主卡最多三张，其余折进「还有 N 件」。今日记下的经验在下面作备忘，不算待办。网页 / PDF / 音频展开「送进来」；掌握仪表和偏好默认收起，顶栏那句趋势仍来自掌握深度/广度。

---

## 建议的第一条路径（约 15 分钟）

1. **确认当前主题**，或在今日桌面点「新建主题」并切换过去。
2. **随手记一条，走「读完这篇 / 会后落地」，或展开「送进来」**（网址 / Markdown / PDF / 音频），点「记下」或「送去过闸」。
3. 材料出现在今日「下一步」：该拒的拒，该过的点「接受并抽取」。抽取只写主张，不自动建章。待挂里点已有概念、当前主题，或原文中出现的新章名（点了才建概念）。主张必须能对上原文，对不上会被丢掉。
4. 中间导图出现概念和主张后，用顶部搜索框搜一个主题内的词，点结果定位。
5. 右侧切到 **学习**，问一个图里有的问题。应看到：直答 / 需要时再查 / 依据 / 联想 / 边界。带对立或练过的主张进直答；细节放在「需要时再查」，不会编一条原理顶上。点主张去「出处」核对原文。
6. 讲解后会出 **一道** 追问。先答对一次，再故意答错一次：图上应多一条误区；再问相邻问题时应被带回。
7. 切到 **决策简报**，看选项、反方、未知。选一项并**自己写理由**（空理由不能写回），之后补事后结果。
8. 主题变大之后，顶栏进 **学习频道**：先看总目录，再从头学一章。中间导图会收起其它章。右侧出处可切到问答；有依据的回答可收入当前章节。

学知识和做决策不在同一条聊天里。「学习」只讲主张；「决策简报」只拍板。切换主题会换一套学习会话。

---

## 功能怎么用

### 学习主题

每个主题是一个 `Goal`：你正在搞清的问题。过闸、搜索、讲解、简报都锁在**当前主题**，避免串题。

- 点已有主题可切换。
- 新建会暂停旧的当前主题。
- 空主题几乎没有图：搜索和学习都会未知，先入库。

### 采集与过闸

左栏今日桌面的随手记一句话也能入库：原文就是证据，过闸后仍要你点接受。抽取只写出带原文位置的主张，不会按模型起名自动建章。「下一步」待挂会先拆成该内化 / 可外置，再列出已有概念、当前主题，以及原文里出现的新章名；点新章名才建概念并写边。若配置了行业检索，会用公开网页片段对照这些已有节点（也可参考偏好里的领域），用来加权挂点；检索结果不是主张、不是目录。要对上某篇网页本身，再点「送去过闸」。

展开「送进来」可提交网页 URL、Markdown、PDF（含扫描件；有模型 Key 时视觉抽字）、音频和视频。音频会转写；视频抽旁白，本机有 ffmpeg 时再抽几帧画面文字。转写不出则拒绝，不会发明没听到的句子。

今日桌面的 **剧本** 不是插件市场，只编排这两条现有路径：

- **读完这篇**：网页或 Markdown 送去过闸 → 下一步里接受抽取 → 挂到已有概念或当前主题。不自动接受，不打开简报。走完之后，当天的召回偏挂在概念上的主张和带对立的主张。
- **会后落地**：记下会上的判断 → 下一步里接受并挂上。勾选后会按当前主题重算简报，仍只用图上已有主张，不会把未过审的会记写进选项。走完之后，当天的召回偏未复盘决策和来自笔记/音视频的可行动主张。

材料先过五道闸（目标 / 质量 / 新颖 / 可行动 / 维护成本），再进人审。清单体、营销稿通常被拒。过闸通过后仍要你点接受，抽取才写图。对人记的短材料会放宽「过短」规则，但模型判定的质量问题仍由代码执行。

没有 `source_id` / 对不上原文的 quote，不能变成已确认主张。

### 图谱与搜索

中间导图总览时主题在上，下一层按几排展示，避免二级节点排成一条长列。点某个节点进入下一级后，只保留当前节点和它的上级 / 上上级，其它兄弟层收起。点「总览」或「上一级」返回。双击钉住后，提问会优先带着这些节点。

顶部搜索只搜**当前主题**，结果分概念 / 主张 / 来源。无命中写未知，不编节点。若这是你正在学的内容，去入库；若只是换了个说法，在导图里找已有点。

### 学习频道

顶栏「学习频道」用来把庞大主题拆成可跟学的目录，而不是另编一本教材：

- 总目录来自图谱里已经抽出的概念；还没有概念层时，按已有主张一节一节读。
- 左栏按章给出装卸清单：该内化（原理 / 对立 / 练过）和可外置（步骤 / 例子 / 出处）。分法来自图结构和练习，你可以改，改完后讲解和搜索跟着走。
- 点「从头学 / 上一章 / 下一章」按顺序走。
- 中间导图跟着当前章收起其它兄弟节点。
- 右侧默认「出处」，可切到「问答」。讲解后仍会出一道追问；你也可以继续问，模型只引用图上主张并反问。
- 「收入当前章节」把这轮问答里**已经引用的主张**用 `about` 挂到当前章。没有可引用的主张时按钮不可用，不会把模型散文写进图。

### 学习会话

右侧「学习」只服务当前主题：

- 讲解分「直答」和「需要时再查」。直答只放带对立或练过的上层主张；细节可点开核对，但不能编原理。各段都必须能点到图上的 id。
- 直答默认跳过 `candidate`（未确认）主张。
- 联想走已有边；若沿边能接到另一个带对立的主张，会写「这和你已有的 X 是同一类取舍」。没有边就停，不会用相似度编迁移。没有边时会说「图上还没连上」，并给出「标成相关」。点一下才会写 `related_to`，不会发明节点。
- 未知时不会用模型私货顶上。下一步是搜已有点或入库。其它主题若有相关主张，会单独列在「其他主题」，勾选「也搜其他主题」才展开，不会默默混进直答。
- 讲解后最多一道追问。批改只判对 / 漏 / 反，标准答案只复述图上主张和原文。
- 答错会把误区挂到它反驳的正确主张上。相关再问会强制带回；连续答对或手动「标记已纠正」后降权，节点仍留在图上。

巩固题出现在今日「下一步」。点过的主张、挂上的主张、拍板时引用的主张会进入巩固，并提高下次召回权重；十分钟内重复点同一条只记一次。到期后点该卡片，仍进入学习会话，不进决策简报。掌握细节在左栏底部「掌握」里展开。

### 决策简报

右侧「决策简报」回答「要不要做 X」，不是「这个知识点是什么」。

简报含选项、证据、未知、最强反对意见。同类问题会带回上次已拍板的决策，不编新结论。采纳必须人手写理由。事后结果也由人写回。这些记录出现在简报页的「本主题决策记录」，不会插进学习会话。

### 掌握仪表

展开左栏「掌握」看深度（主张是否有证据且有对立）和广度（概念覆盖）。状态：

- **掌握**：多次答对且无活跃误区
- **薄弱**：反复误区或批改为「反」
- **缺失**：孤立概念、无对立或无证据

仪表会建议该补对立观点、原始出处还是反例。没有缺口时，应先学已有点，而不是继续狂吃信息。

今日桌面的「下一步」只列当前主题：待挂、待审、到期巩固，然后是已拍板待复盘；简报草稿只在你本周主动重算过之后才出现。主卡最多三张。记下的经验在下面作备忘。可导出一份只引用图上已有 id 的周报。

展开「偏好」只影响排序和口吻（例如更信亲手笔记），不能当答案，也不能填未知。

出处里可以把重复概念合并进已有概念（只加别名、改边，不融合冲突主张），或把过时主张标成 `deprecated`：召回会跳过，历史仍可查。

---

## 空状态和「未知」

| 你看到 | 意思 | 下一步 |
|---|---|---|
| 还没有学习主题 | 过闸和召回没有锚 | 今日桌面点「新建主题」 |
| 主题几乎是空图 | 还没有抽出主张 | 随手记或「送进来」并接受抽取 |
| 搜索 / 学习写未知 | 图上没有可引用的点 | 入库，或改搜已有词 |
| 没有待审材料 | 下一步里没有待审 | 记下或送新材料过闸 |
| 没有误区档案 | 还没被考错过 | 去学习里答一道追问 |
| 没有标出缺口 | 结构上暂时不缺 | 先学已有点，或补对立材料 |
| 图上还没连上 | 有主张但缺边 | 核对后点「标成相关」 |
| 还没有可拆的目录 | 图上没有概念层 | 先入库抽出主张，在待挂里点已有概念或原文中的新章名 |

---

## 安全（单人工具）

没有登录。谁能访问端口，谁就能改你的图谱。本机开发用 http://localhost:3000。不要把未鉴权端口映射到公网。密钥只放在本地 `.env`，不要提交进 Git。

---

## 还不会替你做的事

- 不会上网抓一篇热文直接当你的教材
- 不会在图外生成「标准答案」
- 不会把误区写成没有反驳对象的鸡汤
- 不会替你拍板；空理由不能采纳
