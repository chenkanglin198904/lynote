# LyNote

[English README](README.md) · [简体中文 README](README.zh-CN.md)

个人外脑知识平台。不是模拟世界，而是给一个人做外脑：从海量信息里筛出可复利的知识，在需要时召回带证据的判断，并和人一起做决策。

## 一句话命题

普通人用好 AI 的瓶颈，不是模型不够强，而是知识带宽不够、判断证据不够、跨域连接不够。LyNote 把「学」和「判」做成可复利的闭环。

## 用户

先服务一个人（开发者自己），再开源给「需要用 AI 学习并做工作决策、但不想被信息流淹没」的知识工作者。

垂直切入：个人学习与工作判断。口号可以面向人人可用，产品必须先打穿一条黄金路径。

## 非目标

- 不做第二个 MiroFish：无千人社会模拟、无平行数字世界、无全网舆情双平台。
- 不做笔记搜索套壳：没有主张、冲突、决策节点，就不算本项目。
- 不做全量 embedding 入库：筛选闸门默认拒绝。
- 不替人拍板、不做自动交易、不编造无出处节点。
- 第一版不做完美本体、不做多用户、不做云端默认同步。个人知识默认不出门。

## 四条闭环

1. **入库**：源材料 → 按当前目标筛 → 抽出主张和证据 → 人抽查后入图。
2. **召回**：当前问题 → 混合检索 + 图游走 → 带出处的子图。
3. **决策**：选择 → 选项 / 证据 / 未知 / 最强反对意见 → 人拍板 → 结果回写。
4. **学习**：讲解必须引用图上主张 → 追问考察理解 → 批改并归档个性化误区 → 相关知识点再次召回误区；按掌握 / 薄弱 / 缺失主动推荐补料与复习。

四条环已经接到同一张工作台上。分层 / 装入 / 练习 / 复利接到这四条环上，不另开入口。后续只按 [ROADMAP.md](./ROADMAP.md) 清单往下打，不要另起平行路线。

图谱质量由拒绝率决定，不由摄入量决定。学习正确率由「能否引用 claim id、误区是否挂在正确主张上」决定，不由对话流畅度决定。

## 黄金演示

用户贴 3–5 份材料 + 一个真实问题（默认种子：「要不要把个人知识做成图谱，而不是继续堆笔记」）。系统筛掉无关内容，抽出概念和主张，画出一张小图，给出带出处的决策简报，并列出最强反对意见和未知项。人点「采纳」，`Decision` 写回图谱。

两周内必须能录这条视频。骨架阶段用种子数据先把界面和接口走通。

## 模块边界

| 模块 | 允许 | 禁止 |
|---|---|---|
| `contracts/` | Node / Claim schema 与 API 契约 | 边写边改字段名 |
| `ingest/` | 源 → `SourceDocument`，保留原文定位 | 抽取、写图 |
| `gate/` | 过闸 / 拒绝理由，写入收件箱 | 直接写图 |
| `extract/` | 概念、主张、关系；主张必有 `source_span` | 推荐、生成长文 |
| `graph/` | CRUD + 冲突查询 | 调 LLM |
| `retrieve/` | 混合召回 + 主题内知识点搜索，返回证据子图 / 分栏命中 | 生成长文 |
| `brief/` | 一页简报 + 反方 | 千人模拟 |
| `tutor/` | 四段带证据讲解 + 一道追问；批改只引用图上主张；误区挂在它反驳的主张上并在相关提问强制召回 | 编造标准答案；题海；无证据的鸡汤错法 |
| `learn/` | 掌握 / 薄弱 / 缺失仪表、对比卡片、间隔巩固、按缺口给收件箱排序 | 在图外编标准答案；按热度推荐 |
| `web/` | 三栏工作台 | 把业务逻辑塞进组件 |

## 数据模型（最小完备）

`Source` · `Concept` · `Claim` · `Relation` · `Goal` · `Decision` · `Misconception`

主张是一等公民：没有证据不能进入 `confirmed`。冲突显式保留。决策包含选项、选择、理由、事后结果。误区必须挂到它反驳的 `claim_id`，不能单独作为无证据知识存在。

权威 schema 见 `contracts/schema.json`。Python / TypeScript 类型必须与之对齐。

## 技术选型（骨架）

- 前端：Next.js + React Flow + Tailwind
- 后端：FastAPI
- 图存储：`GRAPH_BACKEND=kuzu` 落盘到 `data/lynote.kuzu`；收件箱 / 对话 / 当前简报写在 `data/workspace.json`。空库才灌种子。测试强制 `memory`，不碰真实数据目录。
- 向量：`providers.VectorStore` 协议。默认 `VECTOR_BACKEND=lancedb` 落盘到 `data/vectors`；测试强制 `memory`。无 embedding Key 时用 hashing 占位。扫描件 PDF 在有 Key 时视觉抽字，不得发明未见文字。
- LLM / Embedding：默认官方 OpenAI（`LLM_API_KEY` + `https://api.openai.com/v1`）。协议兼容任意 `/v1` 网关，改 `LLM_BASE_URL` 即可；Key 留空则规则/本地回退。主张 quote 必须能在原文定位成 `source_span`，否则丢弃。

## 已验证 / 已拒绝

- 已验证：规则闸门拒绝清单体；无 Key 时规则回退仍能抽出带 span 的主张；模型路径下对不上原文的 quote 会被丢弃；重复标题由代码覆盖模型的新颖性判断；裁决由五道闸的 passed 在代码里合成；召回未命中不填垫主张，冲突主张可随边展开；决策简报只引用子图内 claim id，伪造 id 会被丢掉；Kuzu 重开库能还原带 source_span 的主张，sidecar JSON 能还原收件箱状态且不会二次灌种子；已采纳的 Decision 才能写 outcome，空复盘被拒，重算简报会开新决策并保留旧结果；点主张能用已存 source_span 高亮原文，quote 对不上也不重新猜偏移；URL/Markdown/PDF 只抽正文过闸；file 协议拒绝；新建目标会暂停旧的当前目标；空理由不能采纳；知识点搜索锁在当前主题，未知查询不编节点；讲解四段只引用子图 id，直答跳过 candidate，对立主张走已有边；讲解后出一道追问，批改只判对/漏/反且只引用图上 claim id；答错把误区挂到正确主张上，相关再问强制带回，连续答对或手动标记后归档，禁止挂到不存在的 claim；掌握仪表由代码从图结构 + sidecar 练习记录判定，无对立的概念标缺失，补对立的材料在收件箱排在清单体前面；LanceDB 重开目录仍能搜到同一 claim id；无 Key 时空白 PDF 仍拒绝，有视觉回传时扫描件只转录可见文字；「图上还没连上」只对当前主题里已有概念/主张提供「标成相关」，人确认后写 `related_to`，发明 id 或连到来源会被拒绝；讲解写入 learn 车道并挂当前主题，简报采纳/复盘写入 decision 车道，新主题的学习会话为空。
- 已拒绝：先做世界模拟；先做全量向量入库；先做多用户；让模型发明 source_span

## 给后续 AI 的硬规则

1. 图谱不是笔记搜索。缺主张 / 冲突 / 决策，就是跑偏。
2. 筛选不是全收下再 embedding。
3. 无 `source_id` 的结论当 bug。
4. 新 Agent 必须能说出它消灭了哪类错误。
5. 用开发者本周真实的学习/工作问题验收，不要只靠彩排材料。
6. 学习功能按 [ROADMAP.md](./ROADMAP.md) 的切片顺序做；一次只打穿下一项。讲解与批改不得引用子图外的 id。
